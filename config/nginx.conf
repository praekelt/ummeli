# included from /etc/nginx/nginx.conf
# 

upstream ummeli_qa {
    server 127.0.0.1:9101;
}

server {
    listen 80;
    server_name qa.ummeli.praekeltfoundation.org;
    root /var/www/nginx-default/;
    
    location /ummeli-static/ {
        root /var/praekelt/ummeli/ummeli/;
    }
    
    location / {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_pass http://ummeli_qa;
        access_log  /var/log/nginx/qa.ummeli.access.log;
        error_log   /var/log/nginx/qa.ummeli.error.log;
        keepalive_timeout 0;
    }
}

upstream ummeli_mobi_qa {
    server 127.0.0.1:9111;
}

server {
    listen 80;
    server_name m.qa.ummeli.praekeltfoundation.org;
    root /var/www/nginx-default/;
    
    location /ummeli-static/ {
        root /var/praekelt/ummeli/ummeli/;
    }
    
    location / {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_pass http://ummeli_mobi_qa;
        access_log  /var/log/nginx/m.qa.ummeli.access.log;
        error_log   /var/log/nginx/m.qa.ummeli.error.log;
        keepalive_timeout 0;
    }
}
